package com.example.quickcash;

import android.app.job.JobInfo;

public class TempJob {
    //temporary job class for preferred job implementation, replace with 'job' class once integrated

    protected String JobInfo;

    public TempJob(String JobInfo){
        this.JobInfo = JobInfo;
    }
}
