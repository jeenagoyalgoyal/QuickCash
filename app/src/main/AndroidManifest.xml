<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuickCash"
        tools:targetApi="34">

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCV1UHoXi03PIqKcmQLUaXyE23Cu5vIjvk" />

        <!-- Login Activity as Launcher -->
        <activity
            android:name=".ui.activities.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.QuickCash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".ui.activities.MainActivity">
        </activity>

        <!-- Preferred Employers Activity-->
        <activity
            android:name=".ui.activities.PreferredEmployersActivity"
            android:exported="true"
            android:theme="@style/Theme.QuickCash">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Profile activity -->
        <activity
            android:name=".ui.activities.Profile"
            android:exported="true"
            android:theme="@style/Theme.QuickCash">
            </activity>
        <!-- Job Search by Parameter Activity-->
        <activity
            android:name=".ui.activities.JobSearchParameterActivity"
            android:exported="true"
            android:theme="@style/Theme.QuickCash">
        </activity>

        <!--  Maps Activity      -->
        <activity
            android:name=".ui.activities.MapsActivity"
            android:exported="true"
            android:theme="@style/Theme.QuickCash">
        </activity>

        <!-- Registration Activity -->
        <activity
            android:name=".ui.activities.RegistrationActivity"
            android:exported="true"
            android:theme="@style/Theme.QuickCash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Role Selection Activity -->
        <activity
            android:name=".ui.activities.RoleActivity"
            android:exported="true"
            android:theme="@style/Theme.QuickCash" />

        <!-- Map Activity -->
        <activity
            android:name=".ui.activities.MapActivity"
            android:exported="true"
            android:theme="@style/Theme.QuickCash" />

        <!-- Employer Homepage Activity -->
        <activity android:name=".ui.activities.EmployeeHomepageActivity"
            android:exported="true"
            android:theme="@style/Theme.QuickCash" />

        <!-- Employer Homepage Activity -->
        <activity android:name=".ui.activities.EmployerHomepageActivity"
            android:exported="true"
            android:theme="@style/Theme.QuickCash" />

        <!-- JobSubmissionPage Activity -->
        <activity android:name=".ui.activities.JobSubmissionActivity"
            android:exported="true"
            android:theme="@style/Theme.QuickCash">
        </activity>

        <!-- Preferred Jobs Activity -->
        <activity android:name=".ui.activities.PreferredJobsActivity"
            android:exported="true"
            android:theme="@style/Theme.QuickCash">
        </activity>

    </application>

</manifest>